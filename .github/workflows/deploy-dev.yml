name: Deploy Dev (DEPRECATED)

# 옛 host-nginx blue-green 방식은 새 Docker compose 배포(deploy.yml, dev 트리거)로 대체됨.
# 혼동 방지 위해 수동 트리거만 남긴 no-op.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated. 배포는 deploy.yml (Docker compose, dev push) 사용."
