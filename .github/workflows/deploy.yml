name: CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  ci:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build & Test
        run: ./gradlew build

  cd:
    name: Deploy to EC2
    runs-on: ubuntu-latest
    needs: ci
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Add runner IP to security group
        run: |
          RUNNER_IP=$(curl -s https://checkip.amazonaws.com)
          echo "RUNNER_IP=$RUNNER_IP" >> $GITHUB_ENV
          aws ec2 authorize-security-group-ingress \
            --group-id ${{ secrets.SECURITY_GROUP_ID }} \
            --protocol tcp \
            --port 22 \
            --cidr "$RUNNER_IP/32" || true
          echo "✅ Runner IP $RUNNER_IP 보안그룹 허용"
          echo "=== 현재 SG 인바운드 규칙 (port 22) ==="
          aws ec2 describe-security-groups \
            --group-ids ${{ secrets.SECURITY_GROUP_ID }} \
            --query "SecurityGroups[0].IpPermissions[?FromPort==\`22\`]" \
            --output json
          echo "=== EC2 인스턴스에 실제 부착된 Security Groups ==="
          aws ec2 describe-instances \
            --filters "Name=ip-address,Values=${{ secrets.EC2_HOST }}" \
            --query "Reservations[0].Instances[0].SecurityGroups" \
            --output json

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build (skip test)
        run: ./gradlew build -x test

      - name: Build Docker image
        run: docker build -t atomiccv:${{ github.sha }} .

      - name: Save Docker image
        run: docker save atomiccv:${{ github.sha }} | gzip > atomiccv.tar.gz

      - name: Test SSH connectivity
        run: |
          echo "=== EC2 포트 22 연결 테스트 ==="
          nc -zv -w 10 ${{ secrets.EC2_HOST }} 22 && echo "✅ 포트 22 접근 가능" || echo "❌ 포트 22 접근 불가"

      - name: Copy files to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "atomiccv.tar.gz,scripts/deploy.sh"
          target: "~/deploy"

      - name: Execute deploy script
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            chmod +x ~/deploy/scripts/deploy.sh
            ~/deploy/scripts/deploy.sh ${{ github.sha }}

      - name: Remove runner IP from security group
        if: always()
        run: |
          aws ec2 revoke-security-group-ingress \
            --group-id ${{ secrets.SECURITY_GROUP_ID }} \
            --protocol tcp \
            --port 22 \
            --cidr "$RUNNER_IP/32"
          echo "✅ Runner IP $RUNNER_IP 보안그룹 제거"
