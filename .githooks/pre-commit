#!/bin/bash
set -e

echo "▶ ktlint 검사 실행..."
JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew ktlintCheck --daemon -q

if [ $? -ne 0 ]; then
  echo "❌ ktlint 검사 실패 — 코드 스타일 오류를 수정 후 다시 커밋하세요."
  echo "   자동 수정: ./gradlew ktlintFormat"
  exit 1
fi

echo "✅ ktlint 검사 통과"
